<?xml version="1.0" encoding="utf-8"?>
<network-security-config>

    <!-- Default: system CAs only (production safe, no cleartext) -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug builds also trust user-installed certs and the bundled Zscaler root CA -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
            <certificates src="@raw/zscaler_root" />
        </trust-anchors>
    </debug-overrides>

</network-security-config>
